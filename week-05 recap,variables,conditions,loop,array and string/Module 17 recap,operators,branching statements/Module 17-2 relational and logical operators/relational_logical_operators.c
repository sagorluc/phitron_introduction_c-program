#include<stdio.h>
#include<math.h>
int main()
{
    //AND = &&; sobai true hoile true return korbe.otherwise false return korbe
    //OR = ||; sobai false hoile false return korbe.otherwise true return korbe.
    //NOT = !;
    int a;
    a = (6>=6)&&(5<7); //jodi true hoy tahole branch e dhukbe.true means (1) and false means (0).
    printf("%d",a);


    return 0;
}
